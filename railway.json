{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "MAVEN_OPTS='-Xmx512m' mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/*.jar",
    "healthcheckPath": "/actuator/health",
    "restartPolicyType": "ON_FAILURE"
  }
}